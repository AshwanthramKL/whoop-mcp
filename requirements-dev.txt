# Development dependencies for whoop-mcp.
# Runtime deps live in requirements.txt; don't duplicate them here.

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
respx>=0.21.0          # httpx route mocking
freezegun>=1.2.0       # time freezing for daily_summary tests

# Lint, format, type-check
ruff>=0.5.0            # replaces black + isort + flake8
mypy>=1.10.0

# Build + release
build>=0.10.0
twine>=4.0.0

# Pre-commit hook runner
pre-commit>=3.5.0
